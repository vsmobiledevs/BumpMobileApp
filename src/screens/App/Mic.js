import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const Mic = () => {
  return (
    <View>
      <Text>Mic</Text>
    </View>
  );
};

export default Mic;

const styles = StyleSheet.create({});
